[package]
name = "chartreuse-core"
description = "Chartreuse shared types and pure logic"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
thiserror = "2.0.21"

[features]
# Build the release flavor (see `flavor`). Set by `cargo xtask release` only.
release-flavor = []

[lints]
workspace = true
